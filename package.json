{
  "name": "dsh-plugins",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "check": "node scripts/check.mjs",
    "typecheck": "tsc -b packages/quote-select packages/theme-toggle packages/turn-nav packages/web-search"
  },
  "devDependencies": {
    "tsdown": "^0.22.2",
    "typescript": "^6.0.3"
  }
}
